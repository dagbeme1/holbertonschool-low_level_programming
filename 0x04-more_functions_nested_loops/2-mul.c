#include <ctype.h>
#include "main.h"

/**
 * _isdigit - a function that checks for uppercase character
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */

int mul(int a, int b)
{

return (a * b);

}
