#include <ctype.h>
#include "main.h"

/**
 * mul - a function that checks for uppercase character
 * @a: input
 * @b: input
 * Return: 1 if c is uppercase, 0 otherwise
 */

int mul(int a, int b)
{

return (a * b);

}
